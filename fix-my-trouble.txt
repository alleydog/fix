Ошибка при обновлении Deepin v20 (1002)
echo "deb https://uos-packages.deepin.com/printer eagle non-free" | sudo tee /etc/apt/sources.list.d/printer.list

Установка недостающих пакетов
sudo apt install mc lynx htop curl cmus mlocate mtr-tiny mpv (+ kbd dirmngr для Deepin 15.11)

Решение проблем индикатором раскладки Deepin 15.11
pkill dde-dock

Google
wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'

Сервис
sudo nano /etc/systemd/system/fix-my-trouble.service

>> fix-my-trouble.service
[Unit]
Description=Fix My Trouble
After=multi-user.target

[Service]
Type=idle
ExecStart=/etc/systemd/system/fix-my-trouble.sh

[Install]
WantedBy=multi-user.target

Скрипт
sudo nano /etc/systemd/system/fix-my-trouble.sh

>> fix-my-trouble.sh
#!/bin/sh -e
sudo setkeycodes 3a 42
sudo iptables -t mangle -A POSTROUTING -j TTL --ttl-set 65

Права
sudo chmod u+x /etc/systemd/system/fix-my-trouble.sh
sudo chmod 644 /etc/systemd/system/fix-my-trouble.service

Запуск
sudo systemctl daemon-reload
sudo systemctl enable /etc/systemd/system/fix-my-trouble.service

Импорт конфигурационных файлов
nmcli c import type openvpn file ~/.ProtonVPN/jp-free-01.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/jp-free-02.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/jp-free-03.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-01.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-02.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-03.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-04.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-05.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-06.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-07.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-08.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/nl-free-09.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/us-free-01.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/us-free-02.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/us-free-03.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/us-free-04.protonvpn.com.udp.ovpn
nmcli c import type openvpn file ~/.ProtonVPN/us-free-05.protonvpn.com.udp.ovpn
